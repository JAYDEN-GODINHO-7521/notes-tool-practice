/**
 * Root app component. Routing (Login / Dashboard / StudyHub) will be wired
 * up in the frontend-auth-notes todo.
 */
export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center text-slate-600">
      <p>Keep — scaffold running. Routes coming in frontend-auth-notes.</p>
    </div>
  );
}
