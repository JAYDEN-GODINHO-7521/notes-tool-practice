"""Flashcards router: generate/list/delete cards, due list, review submission.

TODO(backend-llm, flashcards-fsrs): implement per REST API contract.
"""
from fastapi import APIRouter

router = APIRouter()
