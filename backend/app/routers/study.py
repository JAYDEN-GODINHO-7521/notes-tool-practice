"""Study Hub router: POST /api/study/session, GET /api/study/stats.

TODO(flashcards-fsrs): implement session shuffle + variant rotation and stats aggregation.
"""
from fastapi import APIRouter

router = APIRouter()
